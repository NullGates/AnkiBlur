--- a/qt/aqt/main.py
+++ b/qt/aqt/main.py
@@ -287,6 +287,7 @@ class MainWindow(QMainWindow):
         self.setupStyle()
         self.setupMenus()
         self.setupTopDeckArea()
+        self.setupTransparency()
         self.setWindowTitle("AnkiBlur")
         self.setMinimumSize(400, 200)

@@ -295,6 +296,18 @@ class MainWindow(QMainWindow):
         self.setWindowIcon(theme_manager.icon_from_resources("anki"))
         self.show()

+    def setupTransparency(self) -> None:
+        """Setup window transparency and blur effects."""
+        # Enable transparency
+        self.setAttribute(Qt.WidgetAttribute.WA_TranslucentBackground, True)
+        self.setWindowFlag(Qt.WindowType.FramelessWindowHint, False)
+
+        # Set window opacity (0.0 = fully transparent, 1.0 = fully opaque)
+        self.setWindowOpacity(0.95)
+
+        # Apply blur effect to background
+        self.setStyleSheet(self.styleSheet() + """
+            QMainWindow {
+                background-color: rgba(255, 255, 255, 240);
+                backdrop-filter: blur(10px);
+            }
+        """)
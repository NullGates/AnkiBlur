--- a/qt/aqt/main.py
+++ b/qt/aqt/main.py
@@ -287,7 +287,7 @@ class MainWindow(QMainWindow):
         self.setupStyle()
         self.setupMenus()
         self.setupTopDeckArea()
-        self.setWindowTitle("Anki")
+        self.setWindowTitle("AnkiBlur")
         self.setMinimumSize(400, 200)

         # Show main window
@@ -1245,9 +1245,9 @@ class MainWindow(QMainWindow):
         """Update window title with current deck info."""
         deck_name = self.col.decks.current()["name"]
         if self.col.sched.counts():
-            title = f"Anki - {deck_name}"
+            title = f"AnkiBlur - {deck_name}"
         else:
-            title = f"Anki - {deck_name} (no cards due)"
+            title = f"AnkiBlur - {deck_name} (no cards due)"
         self.setWindowTitle(title)
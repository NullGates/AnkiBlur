name: build-ankiblur

permissions:
  contents: write

on:
  workflow_dispatch:
    inputs:
      force_version:
        type: boolean
        description: Force update version
      generate_assets:
        type: boolean
        description: Generate assets
      anki_version:
        type: string
        description: Specific Anki version to build
  repository_dispatch:
    types: [new-anki-release]
  push:
    branches: [ main, master ]
    paths-ignore:
    - '**/*.md'
    - 'docs/**'
  pull_request:
    branches: [ main, master ]
    paths-ignore:
    - '**/*.md'

env:
  APP_NAME: AnkiBlur
  BINARY_NAME: ankiblur
  ANKI_REPO: ankitects/anki
  BLUR_REPO: ${{ github.repository }}
  ORG_NAME: ${{ github.repository_owner }}

jobs:
  check:
    runs-on: ubuntu-latest
    outputs:
      ANKI_VERSION: ${{ env.ANKI_VERSION }}
      ANKI_COMMIT: ${{ env.ANKI_COMMIT }}
      BLUR_VERSION: ${{ env.BLUR_VERSION }}
      SHOULD_BUILD: ${{ env.SHOULD_BUILD }}
      SHOULD_DEPLOY: ${{ env.SHOULD_DEPLOY }}

    steps:
      - uses: actions/checkout@v4

      - name: Check Anki version
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          FORCE_VERSION: ${{ github.event.inputs.force_version }}
          ANKI_VERSION_INPUT: ${{ github.event.inputs.anki_version }}
        run: ./scripts/check_version.sh

      - name: Check existing AnkiBlur releases
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: ./scripts/check_existing_releases.sh

  build-launcher:
    needs: check
    if: needs.check.outputs.SHOULD_BUILD == 'yes' || github.event.inputs.generate_assets == 'true'
    runs-on: ubuntu-latest
    env:
      ANKI_VERSION: ${{ needs.check.outputs.ANKI_VERSION }}
      ANKI_COMMIT: ${{ needs.check.outputs.ANKI_COMMIT }}
      BLUR_VERSION: ${{ needs.check.outputs.BLUR_VERSION }}
      SHOULD_DEPLOY: ${{ needs.check.outputs.SHOULD_DEPLOY }}

    steps:
      - uses: actions/checkout@v4

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-unknown-linux-gnu,aarch64-unknown-linux-gnu

      - name: Install cross-compilation dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-aarch64-linux-gnu zstd

      - name: Build universal AnkiBlur launcher
        run: ./scripts/build_launcher.sh

      - name: Create AnkiBlur package definitions
        run: ./scripts/create_ankiblur_packages.sh

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ankiblur-universal-launcher
          path: out/launcher/
          retention-days: ${{ needs.check.outputs.SHOULD_DEPLOY == 'yes' && 30 || 7 }}

  release:
    needs: [check, build-launcher]
    if: needs.check.outputs.SHOULD_DEPLOY == 'yes'
    runs-on: ubuntu-latest
    env:
      BLUR_VERSION: ${{ needs.check.outputs.BLUR_VERSION }}
      ANKI_VERSION: ${{ needs.check.outputs.ANKI_VERSION }}

    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: release-assets/

      - name: Create GitHub Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: ./scripts/create_release.sh

  update-version-tracking:
    needs: [check, release]
    if: needs.check.outputs.SHOULD_DEPLOY == 'yes'
    runs-on: ubuntu-latest
    env:
      BLUR_VERSION: ${{ needs.check.outputs.BLUR_VERSION }}
      ANKI_VERSION: ${{ needs.check.outputs.ANKI_VERSION }}

    steps:
      - uses: actions/checkout@v4

      - name: Update version tracking
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: ./scripts/update_version_tracking.sh